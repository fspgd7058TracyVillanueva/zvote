import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export const Settings: React.FC = () => {
  return (
    <div style={{ padding: 24 }}>
      <Title level={3}>Settings</Title>
      <Card>Settings page (to be implemented).</Card>
    </div>
  );
};


